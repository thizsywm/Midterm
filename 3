widht = int(input('กว้าง'))
hight = int(input('ยาว'))

def area (widht, hight):
    reg = widht * hight
    tri = (1/2) * widht * hight
    return (reg, tri)

area_reg, area_tri = area (widht, hight)
print (f"พื้นที่สี่เหลี่ยม: {area_reg}")
print (f"พื้นที่สามเหลี่ยม: {area_tri}")